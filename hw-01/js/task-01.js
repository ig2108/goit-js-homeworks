'use strict';

const name = 'Генератор защитного поля';

let price = 1000;

let info = `Выбран "${name}", цена за штуку ${price} кредитов`;

console.log(info);

price = 2000;

info = `Выбран "${name}", цена за штуку ${price} кредитов`;

console.log(info);
